<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="/css/normalize.css">
  <link rel="stylesheet" href="/css/application.css">

  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
  <script src="/js/application.js"></script>

  <title>Broccoli Blog</title>
</head>
<body>
  <header role="banner" class="wrap-float bg-white">
    <div class="mar-t-header ">
      <div class="logo-container">
        <img class="logo" src="/images/logo2.png" class="center block">
      </div>
    </div>
      <nav class="clearfix full-nav">
        <ul class="no-bullets no-margin no-padding">
          <li class="pipe-separate t-light-green left home"><a href="/">home</a></li>
          <% if logged_in? %>
          <li class="pipe-separate-no-line left home"><a href="/sessions/new">logout</a></li>
          <li class="pipe-separate t-light-green right home">Hello, <a href="/users/<%= current_user.id %>"><%= current_user.username %></a>!</li>
          <% else %>
          <li class="pipe-separate t-light-green left home"><a href="/users/new">register</a></li>
          <li class="pipe-separate t-light-green left home"><a href="/sessions/new">login</a></li>
          <% end %>
        </ul>
      </nav>
  </header>

  <div class="bg-dk-green pad-t-2 pad-s-1 pad-b-8 mar-b-16 c-white">
    <div class="max-w-700 center">
      <%= yield %>
    </div>
  </div>
</body>
</html>
